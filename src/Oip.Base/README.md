# Oip.Base

Core OIP package with shared ASP.NET Core bootstrapping, module discovery, OpenAPI setup, health checks and common API error handling.

Use it as the foundation for OIP services and modules.
